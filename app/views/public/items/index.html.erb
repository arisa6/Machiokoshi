<div class="container">
  <div class="row">
    
    <div class="col-md-2" style="text-align: center;margin-top:50px;">
      <strong class="border-bottom" style="color: red;">商品を探す</strong>
        <button type="button" class="btn btn-outline-danger"disabled="disabled">ジャンルから選ぶ
          <% @genres.each do |genre| %>
            <%= link_to genre.name, genre_path(genre.id),class: "btn btn-outline-dark w-100" %>
          <% end %>
        </button>
    </div>
    
   <div class="col-md-10" style="text-align: light;margin-top:50px; "> <small class="border-bottom">
     <span style="font-size:25px;"><%= @items_all.count %></span>件あります</small>
      <div class="d-flex flex-wrap justify-content-around">
        <% @items_all.each do |item| %>
          <div class="col-md-3">
              <%= link_to item_path(item) do %>
                <% if item.image.attached? %>
                  <%= image_tag item.image, class: "thumb-160-100"%>
                <% else %>
                  <%= image_tag 'no_image.jpg', class: "thumb-160-100" %>
                <% end %><br> 
                  <small><%= link_to item.name,item_path(item.id)%><br>
                  <%= (item.price) %>円<br></small>
              <% end %>
              
              <!--いいね機能実装予定-->
              <!--<%# if item.favorited_by?(current_customer) %>-->
              <!--  <p>-->
              <!--    <%#= link_to item_favorites_path(item), method: :delete do %>-->
              <!--      <i class="bi bi-suit-heart-fill"></i>-->
              <!--      <%#= item.favorites.count %> いいね-->
              <!--    <%# end %>-->
              <!--  </p>-->
              <!--<%# else %>-->
              <!--  <p>-->
              <!--    <%#= link_to item_favorites_path(item), method: :post do %>-->
              <!--      <i class="bi bi-suit-heart"></i>-->
              <!--      <%#= item.favorites.count %> いいね-->
              <!--    <%# end %>-->
              <!--  </p>-->
              <!--<%# end %>-->
          </div>
        <% end %>
      </div>
        <div class="row">
          <div class="col-md-10" style="text-align: center;margin-top:50px; ">
            <%= paginate @items %>
          </div>
        </div>
   </div>
  </div>
</div>
