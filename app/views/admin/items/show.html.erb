<div class="container">
  <div class="row">
    <div class="col-md-10 offset-1" style="margin-top:30px; ">
      <h1 class="border-bottom">商品詳細</h1>
    </div>
  </div>
  <div class="col-md-2 offset-1" style="text-align: light;">
    <table class="table table-hover">
      <tr>
        <% if @item.image.attached? %>
          <%= image_tag @item.image, size: "200x200" %>
            <% else %>
              <%= image_tag 'no_image', size: "200x200" %>
                <% end %>
      </tr>
    </table>
  </div>
  <div class="col-md-10 offset-1" style="text-align: light;">
    <table class="table">
      <tbody>
        <tr>商品名</tr>
        <td>
          <%= @item.name %>
        </td>
        <tr>商品説明</tr>
        <td>
          <%= @item.introduction %>
        </td>
        <tr> 税込価格<br> (税抜価格) </tr>
        <td>
          <%= @item.add_tax_price.to_s(:delimited) %>
            <%= @item.price.to_s(:delimited) %>円 </td>
        <tr>販売ステータス</tr>
        <td class="font-weight-bold text-success">
          <% if @item.is_active %>
            <%# is_activeの情報を取得して在庫があるかチェック %> 販売中
              <%# trueの表示がでる %>
                <% else %> 販売停止中
                  <%# falseの表示がでる %>
                    <% end %>
        </td>
        <%# if customer_signed_in? %>
          <!--セレクトショップのため不要-->
          <td>
            <%= link_to '編集', edit_admin_item_path(@item), class: "btn btn-success" %>
          </td>
          <td>
            <%= link_to '削除', admin_items_destroy_path(@item), method: :delete, class: "btn btn-danger" %>
          </td>
          <%# end %>
      </tbody>
    </table>
  </div>